import { BoldLinkDirective } from './bold-link.directive';

describe('BoldLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldLinkDirective();
    expect(directive).toBeTruthy();
  });
});
